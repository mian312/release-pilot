generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Release {
  id             Int      @id @default(autoincrement())
  name           String
  date           DateTime
  additionalInfo String?
  steps          Json
}
